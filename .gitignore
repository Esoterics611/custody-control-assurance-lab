# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv / venv  (lockfile IS committed for reproducible installs)
.venv/

# Playwright / Node
node_modules/
tests/e2e/node_modules/
tests/e2e/test-results/
tests/e2e/playwright-report/
tests/e2e/.playwright/

# Reports / artifacts
allure-results/
reports/*.json
reports/*.html
reports/*.sarif
reports/*.xml

# Env / secrets (NEVER commit real secrets — this lab uses none)
.env
